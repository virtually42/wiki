# Codecs

The `Codec[T]` typeclass and its built-in instances.

| Page | Topic |
|------|-------|
| [built-in](built-in.md) | `String`, `Int`/`Long`, `Double`, `Boolean`, `List[T]`, `Map[String, T]` instances |
| [date-time](date-time.md) | `LocalDate`, `LocalTime`, `LocalDateTime`, `OffsetDateTime` (via `PlatformCodecs`) |
| [custom](custom.md) | Writing your own `Codec[T]` and threading error addresses |
