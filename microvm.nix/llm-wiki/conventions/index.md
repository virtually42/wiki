# Conventions

The contract between a microvm.nix runner package and the host module.

| Page | Purpose |
|---|---|
| [runner-layout](runner-layout.md) | Files a runner package must expose under `bin/` and `share/microvm/` |
| [custom-runner](custom-runner.md) | Building runners that boot non-NixOS guests on a microvm.nix host |
